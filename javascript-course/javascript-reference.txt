/**********************************************************************
 JAVASCRIPT MASTER REFERENCE
 Keep this open while learning — explains operators + built-ins
**********************************************************************/



// =====================================================
// VARIABLES
// =====================================================

const constantValue = 10;   // cannot change
let changeableValue = 20;   // can change
var oldStyle = 30;          // avoid using (old JS)



// =====================================================
// DATA TYPES
// =====================================================

let numberType = 5;           // Number
let textType = "Hello";       // String
let booleanType = true;       // Boolean
let nothing = null;           // Intentional empty value
let notAssigned;              // Undefined
let list = [1,2,3];           // Array
let person = {name: "Kevin"}; // Object



// =====================================================
// ARITHMETIC OPERATORS
// =====================================================

+   // addition
-   // subtraction
*   // multiplication
/   // division
%   // remainder (modulus)
**  // power (exponent)

let a = 10 + 5;     // 15
let b = 10 % 3;     // 1
let c = 2 ** 3;     // 8



// =====================================================
// ASSIGNMENT OPERATORS
// =====================================================

let x = 10;

x += 5;   // x = x + 5
x -= 5;   // x = x - 5
x *= 2;   // x = x * 2
x /= 2;   // x = x / 2
x %= 3;   // remainder
x **= 2;  // power



// =====================================================
// INCREMENT / DECREMENT
// =====================================================

x++;  // add 1
x--;  // subtract 1



// =====================================================
// COMPARISON OPERATORS
// =====================================================

==   // equal value (loose)
===  // equal value + type (STRICT — USE THIS)
!=   // not equal
!==  // strict not equal
>    // greater than
<    // less than
>=   // greater or equal
<=   // less or equal

5 == "5"   // true  (bad)
5 === "5"  // false (good)



// =====================================================
// LOGICAL OPERATORS
// =====================================================

&&  // AND (both true)  checks both conditions
||  // OR (one true)
!   // NOT (reverse boolean)



// =====================================================
// STRING OPERATIONS
// =====================================================

let first = "Kevin";
let last = "Wingfield";

first + " " + last
`${first} ${last}`     // template literal (BEST)


// STRING METHODS
first.length
first.toUpperCase()
first.toLowerCase()
first.includes("v")
first.startsWith("K")
first.endsWith("n")
first.slice(0,3)
first.replace("Kevin","Bob")
first.split("")



// =====================================================
// NUMBER METHODS
// =====================================================

Number("10")      // convert string to number
parseInt("10px")
parseFloat("10.5")

(10.234).toFixed(2)
Math.round(4.6)
Math.floor(4.9)
Math.ceil(4.1)
Math.max(1,5,3)
Math.min(1,5,3)
Math.random()



// =====================================================
// ARRAY METHODS (VERY IMPORTANT)
// =====================================================

let arr = [1,2,3];

arr.push(4)        // add to end
arr.pop()          // remove last
arr.unshift(0)     // add to front
arr.shift()        // remove first

arr.includes(2)
arr.indexOf(3)
arr.join(",")
arr.slice(1,3)
arr.splice(1,1)

arr.forEach(x => console.log(x))
arr.map(x => x * 2)
arr.filter(x => x > 1)
arr.find(x => x === 2)
arr.reduce((a,b)=>a+b,0)



// =====================================================
// OBJECT METHODS
// =====================================================

let user = {name:"Kevin", age:46};

user.name
user["age"]

Object.keys(user)
Object.values(user)
Object.entries(user)



// =====================================================
// TYPE CHECKING
// =====================================================

typeof 5            // "number"
typeof "hi"         // "string"
typeof true         // "boolean"
Array.isArray(arr)  // true



// =====================================================
// CONDITIONALS
// =====================================================

if (age > 18) {}
else if (age === 18) {}
else {}


// ternary operator
age > 18 ? "adult" : "minor";



// =====================================================
// LOOPS
// =====================================================

for (let i=0;i<5;i++) {}

while (x < 10) {}

for (const item of arr) {}



// =====================================================
// FUNCTIONS
// =====================================================

// normal
function add(a,b){
  return a+b;
}

// arrow (modern)
const add2 = (a,b)=>a+b;



// =====================================================
// TRUTHY / FALSY VALUES
// =====================================================

// FALSY:
false
0
""
null
undefined
NaN

// everything else = TRUE



// =====================================================
// ASYNC (IMPORTANT LATER)
// =====================================================

async function getData(){
  const data = await fetch("url");
  const json = await data.json();
}



// =====================================================
// DOM (BROWSER ONLY)
// =====================================================

document.querySelector("#id")
document.querySelectorAll(".class")
element.textContent = "Hello"
element.addEventListener("click", ()=>{})



/**********************************************************************
 END OF REFERENCE
**********************************************************************/
